#include<bits/stdc++.h>
using namespace std;
/*
int main()

{
    float interestAmount, timePeriod, rateOfInterest;
    cin >> interestAmount >> timePeriod >> rateOfInterest;

    float requiredAmount = interestAmount * timePeriod;
    float temp =  requiredAmount;
    float interestAmountOnRequiredAmount;

    while(timePeriod)
    {
        interestAmountOnRequiredAmount = (requiredAmount * rateOfInterest)/1200;

        float money = requiredAmount + interestAmountOnRequiredAmount;
        requiredAmount = money - interestAmount;
        timePeriod--;
    }

    cout << round(temp - requiredAmount);
    return 0;
}

*/
int main()
{
    float moneyPerMonth, interestRatePerAnnum;
    int durationInMonths;
    cin >> moneyPerMonth >> durationInMonths >> interestRatePerAnnum;

    //computation
    float fixedAmount, principalAmount, interestPerMonth;

    fixedAmount = principalAmount + principalAmount *  
    
    return 0;
}